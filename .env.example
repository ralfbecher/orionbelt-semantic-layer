# Server Configuration
# -------------------
# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# REST API Configuration
API_SERVER_HOST=localhost
API_SERVER_PORT=8000

# MCP Transport Configuration
# Options: stdio, http, sse
# - stdio: Standard I/O transport (default, for Claude Code / Cursor)
# - http: Streamable HTTP transport
# - sse: Server-Sent Events (legacy)
MCP_TRANSPORT=http

# MCP Server configuration (only used for http/sse transports)
MCP_SERVER_HOST=localhost
MCP_SERVER_PORT=9000

# Session Configuration
# TTL in seconds before an idle session is purged (default: 1800 = 30 min)
SESSION_TTL_SECONDS=1800
# Interval in seconds between cleanup sweeps (default: 60)
SESSION_CLEANUP_INTERVAL=60

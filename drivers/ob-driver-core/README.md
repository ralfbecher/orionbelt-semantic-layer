# ob-driver-core

Shared foundation for OrionBelt DB-API 2.0 drivers.

Provides PEP 249 exception hierarchy, type objects, OBML YAML detection,
and the compilation bridge (direct import or REST fallback).
